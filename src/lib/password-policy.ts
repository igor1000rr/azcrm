// Политика паролей — единый источник истины для всех мест валидации.
//
// 06.05.2026 — пункт #33 аудита.
//
// Изначально PASSWORD_MIN_LENGTH была экспортирована из 'use server' файла
// (settings/team/actions.ts), но Next.js это запрещает — из 'use server' можно
// экспортировать ТОЛЬКО async функции (билд падал с ошибкой). Поэтому
// константа вынесена в отдельный модуль — её можно импортировать как из серверных
// actions, так и из client components.
//
// 12 символов — рекомендация NIST SP 800-63B для паролей без 2FA.

export const PASSWORD_MIN_LENGTH = 12;
